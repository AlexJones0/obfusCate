# TODO[TESTING] test complexity metrics here. For each metric, just check that
# the valid metrics exist and equal some known values for a known program.
# Should then be like 10 tests (1 values exist, 2 are correct for each) and
# won't be much effort.
